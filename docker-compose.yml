version: "3.8"
services:
  livekit-agent:
    image: my-livekit-agent:latest
    restart: always
    environment:
      LIVEKIT_URL: "wss://testing-voice-agents-rgwx8hsx.livekit.cloud"
      LIVEKIT_API_KEY: "APIyu3V7R6xnZYp"
      LIVEKIT_API_SECRET: "JYAij9a6s1MR0WewhRVrh06mnM8vMtaO75IQjmfgWPk"
  # OPENAI_API_KEY should be set via environment or .env file, not committed here
